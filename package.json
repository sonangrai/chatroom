{
  "name": "socketchat",
  "version": "1.0.0",
  "description": "",
  "main": "src/main.ts",
  "scripts": {
    "build": "NPM_CONFIG_PRODUCTION=false npm install --prefix chat && npm run build --prefix chat",
    "prestart": "npm run build",
    "client": "npm start --prefix chat",
    "start:dev": "concurrently \"nodemon src/main.js\" \" npm run client\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "concurrently": "^6.4.0",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "socket.io": "^4.4.0"
  }
}
